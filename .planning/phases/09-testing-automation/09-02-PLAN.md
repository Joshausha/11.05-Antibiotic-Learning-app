---
phase: 09-testing-automation
plan: 02
type: execute
---

<objective>
Configure Jest coverage collection, reporters, and quality gates.

Purpose: Set up comprehensive test coverage tracking with quality thresholds to prevent coverage regression.
Output: Enhanced jest.config.js with coverage collection, multiple reporters, and coverage thresholds matching project standards.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-automation/09-RESEARCH.md
@.planning/phases/09-testing-automation/09-01-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

**Current Jest setup:**
- Jest 27.4.3 configured in package.json "jest" section
- Test environment: jsdom
- No explicit coverage configuration yet
- Current coverage: 80%+ overall, 98% test pass rate

**Research findings:**
- Use Jest's native `coverageThreshold` for quality gates
- Reporters: text, text-summary, lcov (for Codecov), json-summary (for badges)
- collectCoverageFrom: Include all source files, exclude tests and stories
- Critical medical data: Higher coverage threshold (90%) for src/data/**
- Global threshold: 70% (current - 10% buffer)
- CI-specific reporters: github-actions for inline annotations

**Files to read:**
@package.json (current Jest config section)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Jest config to jest.config.js</name>
  <files>jest.config.js, package.json</files>
  <action>
    Create `jest.config.js` in project root with full Jest configuration:

    1. Move existing Jest config from package.json "jest" section to jest.config.js
    2. Add comprehensive coverage configuration following 09-RESEARCH.md patterns:
       - testEnvironment: 'jsdom' (existing)
       - collectCoverage: true
       - collectCoverageFrom: ['src/**/*.{js,jsx,ts,tsx}', exclude tests/stories/index]
       - coverageDirectory: 'coverage'
       - coverageReporters: ['text', 'text-summary', 'lcov', 'json-summary']
       - coverageThreshold: global 70%, src/data/** 90% (medical data - higher standard)
       - reporters: ['default', ['github-actions', { silent: false }], 'summary']
       - maxWorkers: '50%' (CI optimization)
       - cache: true
    3. Remove "jest" section from package.json
    4. Keep existing setupFilesAfterEnv, moduleNameMapper, and transform settings

    Medical data gets 90% threshold because it's safety-critical. Global threshold 70% preserves current coverage level with buffer. Avoid jest-github-actions reporter package (not needed - use github-actions reporter name directly).
  </action>
  <verify>
    - `jest.config.js` exists and exports valid configuration
    - `package.json` no longer has "jest" section (moved to jest.config.js)
    - Run `npm test` to verify tests still pass with new configuration
    - Coverage thresholds defined: global 70%, src/data/** 90%
  </verify>
  <done>
    Jest configuration extracted to jest.config.js with comprehensive coverage settings. Tests pass with new config. Coverage collection enabled with quality thresholds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test workflow to run with coverage</name>
  <files>.github/workflows/test.yml</files>
  <action>
    Update the test workflow to run tests with coverage:
    - Change test command from `npm test` to `npm test -- --coverage --maxWorkers=2`
    - Coverage will now run automatically in CI using jest.config.js settings
    - maxWorkers=2 limits parallelism for CI environment stability

    Keep matrix testing, caching, and other existing workflow features. Just enhance the test command to include coverage.
  </action>
  <verify>
    - Workflow file shows `npm test -- --coverage --maxWorkers=2`
    - `git diff .github/workflows/test.yml` shows coverage flag added
    - Run `npm test -- --coverage --maxWorkers=2` locally to verify coverage output
  </verify>
  <done>
    Test workflow enhanced to run with coverage. Coverage reports will be generated in CI. Quality gates will fail build if coverage drops below thresholds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add test:ci script and commit changes</name>
  <files>package.json, jest.config.js, .github/workflows/test.yml</files>
  <action>
    Add convenience script to package.json and commit all changes:
    1. Add script: `"test:ci": "jest --coverage --watchAll=false --maxWorkers=2"`
    2. Update workflow to use `npm run test:ci` instead of `npm test -- --coverage --maxWorkers=2`
    3. Stage files: `git add jest.config.js package.json .github/workflows/test.yml`
    4. Commit: "ci(09-02): configure Jest coverage with quality gates"

    This creates a clean separation: `npm test` for local dev (fast), `npm run test:ci` for CI (with coverage).
  </action>
  <verify>
    - `package.json` has "test:ci" script
    - `.github/workflows/test.yml` uses `npm run test:ci`
    - All files committed
    - `git log -1 --oneline` shows coverage configuration commit
  </verify>
  <done>
    Jest coverage fully configured with quality gates. CI will fail if coverage drops below 70% global or 90% for medical data. Coverage reports generated in multiple formats for visualization tools.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `jest.config.js` exists with coverage configuration
- [ ] Coverage thresholds: 70% global, 90% src/data/**
- [ ] Coverage reporters include text, lcov, json-summary
- [ ] `npm run test:ci` command works locally
- [ ] Test workflow uses `npm run test:ci`
- [ ] All changes committed to git
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Jest coverage fully configured
- Quality gates will prevent coverage regression
- CI generates coverage reports in multiple formats
  </success_criteria>

<output>
After completion, create `.planning/phases/09-testing-automation/09-02-SUMMARY.md`:

# Phase 09-02: Jest Coverage Configuration Summary

**Comprehensive Jest coverage setup with quality thresholds and multi-format reporting**

## Accomplishments

- Extracted Jest config from package.json to dedicated jest.config.js
- Configured coverage collection for all source files (excluding tests/stories)
- Set quality gates: 70% global coverage, 90% for critical medical data
- Added multiple coverage reporters: text, lcov, json-summary for tooling integration
- Created test:ci script for CI-optimized test runs
- Updated GitHub Actions workflow to run with coverage

## Files Created/Modified

- `jest.config.js` - New comprehensive Jest configuration with coverage settings
- `package.json` - Removed jest section, added test:ci script
- `.github/workflows/test.yml` - Updated to run tests with coverage

## Decisions Made

- 70% global coverage threshold (current - 10% buffer for safety margin)
- 90% threshold for src/data/** (medical data is safety-critical)
- maxWorkers=2 in CI for stability (vs 50% which can vary)
- Separate test:ci script for clean local vs CI distinction

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 09-03-PLAN.md (Coverage badge generation)
</output>
