---
phase: 09-testing-automation
plan: 04
type: execute
---

<objective>
Add automated PR coverage comments for immediate feedback on code changes.

Purpose: Provide coverage diff comments on pull requests showing coverage changes before merge.
Output: Automated PR comments with coverage comparison, file-level coverage details, and pass/fail status.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-automation/09-RESEARCH.md
@.planning/phases/09-testing-automation/09-03-SUMMARY.md
@.planning/codebase/TESTING.md

**Current state:**
- Jest coverage configured with json-summary reporter (09-02)
- Coverage badges configured for main branch (09-03)
- Test workflow runs on PRs and pushes

**Research findings:**
- Use ArtiomTr/jest-coverage-report-action for PR comments
- Don't hand-roll GitHub API comment scripts (causes duplicate comments, broken updates)
- Action automatically handles: comment updates, deduplication, coverage diff formatting
- Only run on PRs (not main branch pushes)
- Threshold: 70% (matching jest.config.js global threshold)

**Key insight from research:**
Marketplace actions solve "coverage visualization" problem better than custom scripts. They handle comment updates, GitHub API changes, and edge cases.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PR coverage comment step to workflow</name>
  <files>.github/workflows/test.yml</files>
  <action>
    Add PR coverage comment step to test workflow:
    - Add step after test execution, after badge generation step
    - Use ArtiomTr/jest-coverage-report-action@v2
    - Conditional: `if: matrix.node-version == '20.x' && github.event_name == 'pull_request'`
    - Configuration:
      - github-token: ${{ secrets.GITHUB_TOKEN }} (automatically available)
      - threshold: 70 (matches jest.config.js global threshold)
      - working-directory: ./
      - coverage-file: ./coverage/coverage-summary.json
      - base-coverage-file: ./coverage/coverage-summary.json

    Only run on Node 20.x (avoid duplicate comments). Only run on pull_request events (not pushes).

    Note: This action provides coverage diff, file-level coverage, and pass/fail based on threshold.
  </action>
  <verify>
    - Workflow has PR coverage comment step
    - Step conditional on Node 20.x and pull_request event
    - Uses ArtiomTr/jest-coverage-report-action@v2
    - Threshold set to 70
    - `git diff .github/workflows/test.yml` shows new comment step
  </verify>
  <done>
    PR coverage comments configured. Will post coverage diff on all pull requests with threshold-based pass/fail status.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit PR comment setup and create phase summary</name>
  <files>.github/workflows/test.yml</files>
  <action>
    Commit final testing automation configuration:
    - Stage workflow: `git add .github/workflows/test.yml`
    - Commit: "ci(09-04): add PR coverage comments for immediate feedback"
    - Docs commit: `git add .planning/phases/09-testing-automation/09-04-SUMMARY.md` (when created)
    - Docs commit message: "docs(09): complete testing automation & coverage phase"
  </action>
  <verify>
    - Workflow changes committed
    - `git log -1 --oneline` shows PR comment configuration
    - Phase 9 complete with 4 plans
  </verify>
  <done>
    Testing automation complete. CI now provides: matrix testing, coverage quality gates, badges, and PR coverage comments. Phase 9 fully executed.
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Test workflow has PR comment step
- [ ] Comment step conditional on Node 20.x and PRs only
- [ ] Threshold matches jest.config.js (70%)
- [ ] All workflow changes committed
- [ ] Phase 9 plans: 01 (workflow), 02 (config), 03 (badges), 04 (comments) all complete
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- PR coverage comments configured
- Testing automation fully operational
- Phase 9 complete: CI tests with coverage, badges, quality gates, and PR feedback
  </success_criteria>

<output>
After completion, create `.planning/phases/09-testing-automation/09-04-SUMMARY.md`:

# Phase 09-04: PR Coverage Comments Summary

**Automated pull request coverage comments with diff and threshold validation**

## Accomplishments

- Added ArtiomTr/jest-coverage-report-action to test workflow
- Configured PR coverage comments with 70% threshold matching jest.config.js
- Comments include: coverage diff, file-level coverage, pass/fail status
- Only runs on PRs to avoid noise on main branch pushes

## Files Created/Modified

- `.github/workflows/test.yml` - Added PR coverage comment step

## Decisions Made

- Use ArtiomTr action over custom scripts (handles comment updates, deduplication)
- 70% threshold matches global coverage requirement
- Only run on Node 20.x PRs (avoid duplicate comments)

## Issues Encountered

[Problems and resolutions, or "None"]

## Phase 9 Complete

Phase 9 (Testing Automation & Coverage) is now complete with:

**Infrastructure (09-01):**
- GitHub Actions test workflow with matrix testing
- Node 18.x and 20.x support
- npm caching for 40-60% faster CI

**Quality Gates (09-02):**
- Jest coverage configuration with thresholds
- 70% global coverage, 90% for medical data
- Multi-format reporters (text, lcov, json-summary)

**Visualization (09-03):**
- Automated coverage badge generation
- Badge auto-updates on main branch pushes
- README displays current coverage

**Feedback (09-04):**
- PR coverage comments on every pull request
- Coverage diff shows impact of changes
- Threshold-based pass/fail validation

## Next Phase Readiness

Testing automation foundation complete. Ready for Phase 10 (Build Optimization & Caching).

Considerations for future enhancements:
- Codecov integration for historical coverage tracking (if needed)
- Flaky test detection (if CI reliability becomes an issue)
- Test sharding for faster parallel execution (if test suite grows significantly)

---
*Phase: 09-testing-automation*
*Completed: 2026-01-08*
</output>
