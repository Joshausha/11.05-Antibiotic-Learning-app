---
phase: 09-testing-automation
plan: 03
type: execute
---

<objective>
Add coverage badge generation to README with automatic updates on main branch.

Purpose: Provide visual coverage status in README that updates automatically on every main branch push.
Output: Coverage badge in README.md that displays current test coverage percentage.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-automation/09-RESEARCH.md
@.planning/phases/09-testing-automation/09-02-SUMMARY.md
@.planning/codebase/TESTING.md

**Current state:**
- Jest configured with json-summary reporter (09-02)
- Coverage runs in CI via test workflow
- No badges in README yet

**Research findings:**
- Use jpb06/coverage-badges-action for badge generation
- Don't hand-roll SVG generation or badge hosting
- Badges stored in separate branch (e.g., 'badges' branch)
- Only run on main branch pushes (not PRs)
- Requires coverage/coverage-summary.json from Jest

**Files to read:**
@README.md (to add badge)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coverage badge generation to workflow</name>
  <files>.github/workflows/test.yml</files>
  <action>
    Add coverage badge generation step to test workflow:
    - Add step after coverage runs, before end of job
    - Use jpb06/coverage-badges-action@v1
    - Only run on: main branch pushes, Node 20.x matrix (avoid duplicate runs)
    - Configuration:
      - branches: main (or master if that's the primary branch name)
      - coverage-summary-path: ./coverage/coverage-summary.json
      - target-branch: badges (will create this branch automatically)
    - Condition: `if: matrix.node-version == '20.x' && github.ref == 'refs/heads/main'`

    Check current main branch name with `git branch --show-current` or `git symbolic-ref refs/remotes/origin/HEAD`. Use the actual primary branch name (likely 'main').

    Action creates badges branch and commits badge SVGs automatically. No manual setup required.
  </action>
  <verify>
    - Workflow has coverage badge generation step
    - Step conditional on Node 20.x and main branch
    - Uses jpb06/coverage-badges-action@v1
    - `git diff .github/workflows/test.yml` shows new badge step
  </verify>
  <done>
    Badge generation configured in workflow. Will create badges branch and update badge SVGs automatically on main branch pushes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add coverage badge to README</name>
  <files>README.md</files>
  <action>
    Add coverage badge to README.md near the top (after title, before description):
    - Badge format: `![Coverage](https://raw.githubusercontent.com/{owner}/{repo}/badges/coverage.svg)`
    - Replace {owner} and {repo} with actual GitHub repository owner and name
    - Determine from git remote: `git remote get-url origin`
    - Parse owner/repo from URL (e.g., github.com:joshpankin/Antibiotic_Learning_App.git → joshpankin/Antibiotic_Learning_App)

    Place badge in a "Badges" section or inline after the main heading. Standard placement is after the title before the main content.

    Note: Badge won't display until workflow runs and creates badges branch on first main push.
  </action>
  <verify>
    - README.md has coverage badge image markdown
    - Badge URL points to correct GitHub repo
    - Badge references 'badges' branch and 'coverage.svg' file
    - `git diff README.md` shows badge addition
  </verify>
  <done>
    Coverage badge added to README. Badge will display after first successful main branch workflow run creates badges branch.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit badge generation setup</name>
  <files>.github/workflows/test.yml, README.md</files>
  <action>
    Commit badge generation configuration:
    - Stage files: `git add .github/workflows/test.yml README.md`
    - Commit: "ci(09-03): add coverage badge generation to README"
    - Note in commit body: Badge will display after first main branch push creates badges branch
  </action>
  <verify>
    - Both files committed
    - `git log -1 --oneline` shows badge generation commit
  </verify>
  <done>
    Coverage badge generation fully configured. Next main branch push will create badges branch and display coverage percentage in README.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Test workflow has badge generation step
- [ ] Badge step conditional on Node 20.x and main branch
- [ ] README.md has coverage badge markdown
- [ ] Badge URL points to correct repo and badges branch
- [ ] All changes committed to git
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Badge generation configured in CI
- README displays badge (will show after first main push)
- No manual badge updates required
  </success_criteria>

<output>
After completion, create `.planning/phases/09-testing-automation/09-03-SUMMARY.md`:

# Phase 09-03: Coverage Badge Generation Summary

**Automated coverage badge generation and README integration**

## Accomplishments

- Added jpb06/coverage-badges-action to test workflow
- Configured badge generation to run only on main branch Node 20.x builds
- Added coverage badge to README.md
- Badges will auto-update on every main branch push

## Files Created/Modified

- `.github/workflows/test.yml` - Added badge generation step
- `README.md` - Added coverage badge display

## Decisions Made

- Use badges branch for badge storage (GitHub Pages alternative)
- Only generate badges on main branch to avoid PR noise
- Use Node 20.x for badge generation (latest LTS)

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 09-04-PLAN.md (PR coverage comments)
</output>
