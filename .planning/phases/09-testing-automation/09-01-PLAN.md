---
phase: 09-testing-automation
plan: 01
type: execute
---

<objective>
Create GitHub Actions test workflow with Node.js caching and matrix testing.

Purpose: Automate test execution in CI for every push and pull request with caching for fast feedback.
Output: Working `.github/workflows/test.yml` with matrix testing across Node 18.x and 20.x.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-automation/09-RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

**From Phase 8 (Learning Analytics):**
@.planning/phases/08-learning-analytics/08-02-SUMMARY.md

**Current testing setup:**
- Jest 27.4.3 with React Testing Library
- Test command: `npm test` (runs `node scripts/test.js --watchAll=false`)
- 80%+ test coverage, 98% individual test pass rate
- Co-located `__tests__/` directories pattern

**Research findings (09-RESEARCH.md):**
- Standard stack: Jest built-in coverage + GitHub Actions setup-node v4 + actions/cache v4
- Pattern: Matrix testing across Node 18.x and 20.x with npm caching
- Don't hand-roll: Use GitHub Actions marketplace for test runners
- Pitfall: Must use `npm ci` not `npm install` for lock file consistency
- Architecture: Start with basic test workflow before adding coverage reporting
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions test workflow</name>
  <files>.github/workflows/test.yml</files>
  <action>
    Create `.github/workflows/test.yml` following GitHub Actions Node.js best practices:
    - Trigger on push to main/master branches and all pull requests
    - Matrix strategy testing Node 18.x and 20.x (fail-fast: false)
    - Use actions/checkout@v4 for code checkout
    - Use actions/setup-node@v4 with `cache: 'npm'` for dependency caching
    - Run `npm ci` (NOT `npm install`) for lock file consistency
    - Run `npm test` for test execution
    - Set permissions: contents: read, pull-requests: write, checks: write

    Use official GitHub Actions Node.js guide pattern from 09-RESEARCH.md. Avoid custom test runners - Jest is already configured via package.json.
  </action>
  <verify>
    - File `.github/workflows/test.yml` exists
    - Workflow has matrix strategy with node-version: ['18.x', '20.x']
    - Uses `npm ci` not `npm install`
    - Uses `cache: 'npm'` in setup-node step
    - Triggers on push (main branches) and pull_request
  </verify>
  <done>
    GitHub Actions test workflow created with matrix testing and npm caching. Workflow validates via `cat .github/workflows/test.yml` showing correct structure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify workflow syntax and commit</name>
  <files>.github/workflows/test.yml</files>
  <action>
    Verify the workflow file syntax is valid YAML, then commit:
    - Check YAML syntax with `cat .github/workflows/test.yml` (visual inspection)
    - Stage the workflow file: `git add .github/workflows/test.yml`
    - Commit with message: "ci(09-01): add GitHub Actions test workflow with matrix testing"
    - Note: Workflow will run on next push to verify it works end-to-end
  </action>
  <verify>
    - Workflow file committed to git
    - `git log -1 --oneline` shows commit with "ci(09-01): add GitHub Actions test workflow"
    - Workflow syntax appears valid (no obvious YAML errors)
  </verify>
  <done>
    Test workflow committed. Will trigger on next push to validate execution. Workflow includes Node 18.x/20.x matrix testing with npm caching for 40-60% faster CI runs.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.github/workflows/test.yml` exists with valid YAML syntax
- [ ] Matrix strategy includes Node 18.x and 20.x
- [ ] Uses `npm ci` for dependency installation
- [ ] Uses `cache: 'npm'` for dependency caching
- [ ] Workflow committed to git
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Test workflow created following GitHub Actions best practices
- Workflow ready to run on next push
- No syntax errors in workflow file
  </success_criteria>

<output>
After completion, create `.planning/phases/09-testing-automation/09-01-SUMMARY.md`:

# Phase 09-01: GitHub Actions Test Workflow Summary

**GitHub Actions test workflow with Node 18.x/20.x matrix testing and npm caching**

## Accomplishments

- Created `.github/workflows/test.yml` with matrix testing across Node versions
- Configured npm caching for 40-60% faster CI runs
- Set up proper permissions for test annotations and PR comments
- Used `npm ci` for lock file consistency following best practices

## Files Created/Modified

- `.github/workflows/test.yml` - New GitHub Actions workflow for automated testing

## Decisions Made

- Matrix test across Node 18.x and 20.x (LTS compatibility)
- Use fail-fast: false to continue testing other versions if one fails
- Standard GitHub Actions marketplace actions (no custom runners)

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 09-02-PLAN.md (Jest coverage configuration)
</output>
