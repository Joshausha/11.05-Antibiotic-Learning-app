---
phase: 05-enhanced-network-interactivity
plan: 01
type: execute
---

<objective>
Add selection state management and fade system for click-to-explore interactions.

Purpose: Enable visual exploration of antibiotic-pathogen relationships through click-to-select drill-down.
Output: Network nodes can be selected, with unrelated nodes/edges fading to 30% opacity.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-spaced-repetition-integration/05-CONTEXT.md

# Prior phase patterns:
@.planning/phases/02-visual-network-exploration/02-02-SUMMARY.md (centralized state pattern)

# Source files:
@src/hooks/useD3ForceSimulation.ts
@src/components/visualizations/D3NetworkGraph.tsx
@src/types/network-ui.types.ts

**Tech stack available:** D3.js, React hooks, TypeScript
**Established patterns:** Single-object state (NetworkFilters), useMemo optimization
**Constraining decisions:**
- Phase 02-02: Use single state object instead of separate state variables
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selection state to network types and hook</name>
  <files>src/types/network-ui.types.ts, src/hooks/useNetworkSelection.ts</files>
  <action>
1. Add NetworkSelectionState interface to network-ui.types.ts:
   - selectedNodeId: string | null
   - connectedNodeIds: Set<string> (nodes connected to selected node)
   - connectedEdgeIds: Set<string> (edges connecting to selected node)

2. Create useNetworkSelection custom hook:
   - State: selectedNodeId, connectedNodeIds, connectedEdgeIds
   - selectNode(nodeId: string, edges: NetworkEdge[]): Calculate connected nodes/edges from edge list
   - clearSelection(): Reset all to null/empty
   - isNodeConnected(nodeId: string): boolean helper
   - isEdgeConnected(edgeId: string): boolean helper

The hook should compute connected IDs on selection change using useMemo. For a selected pathogen, connected nodes are antibiotics that cover it. For a selected antibiotic, connected nodes are pathogens it covers. Use the edges array to determine connections.

AVOID: Separate state variables - use single NetworkSelectionState object per Phase 02-02 pattern.
  </action>
  <verify>TypeScript compiles without errors: npx tsc --noEmit</verify>
  <done>
- NetworkSelectionState interface exported from network-ui.types.ts
- useNetworkSelection hook exports selectNode, clearSelection, and connection helpers
- Connected nodes/edges calculated correctly based on edge relationships
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement opacity-based fade effect in D3NetworkGraph</name>
  <files>src/components/visualizations/D3NetworkGraph.tsx</files>
  <action>
1. Import and use useNetworkSelection hook in D3NetworkGraph

2. Add opacity calculation logic:
   - When no node selected: all nodes/edges at 100% opacity
   - When node selected:
     - Selected node: 100% opacity
     - Connected nodes: 100% opacity
     - Connected edges: 100% opacity
     - Unconnected nodes: 30% opacity
     - Unconnected edges: 15% opacity (more faded than nodes)

3. Apply opacity to SVG elements:
   - For circle elements: add fillOpacity attribute
   - For line elements: add strokeOpacity attribute
   - Use helper functions from useNetworkSelection to determine connected status

4. The opacity values should be applied directly via inline styles or attributes (no CSS transitions yet - that's Plan 05-02).

AVOID: Adding click handlers in this task - that's Task 1 of Plan 05-02. This task only adds the visual fade based on selection state (which starts as null = everything visible).
  </action>
  <verify>npm run build succeeds; manually test by temporarily hardcoding a selectedNodeId to verify fade works</verify>
  <done>
- D3NetworkGraph uses useNetworkSelection hook
- Opacity correctly applied based on selection state
- When selection is null, all elements are at 100% opacity
- When selection exists, connected elements stay bright, unconnected elements fade
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without errors: `npx tsc --noEmit`
- [ ] Build succeeds: `npm run build`
- [ ] Network renders correctly with no selection (all nodes/edges visible at 100%)
- [ ] Selection state hook properly calculates connected nodes/edges
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Selection state management implemented with centralized state pattern
- Fade effect correctly dims unconnected elements
</success_criteria>

<output>
After completion, create `.planning/phases/05-spaced-repetition-integration/05-01-SUMMARY.md`:

# Phase 5 Plan 1: Selection State & Fade System Summary

**[One-liner describing what was accomplished]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 05-02-PLAN.md (Bidirectional Drill-Down & Animation)
</output>
