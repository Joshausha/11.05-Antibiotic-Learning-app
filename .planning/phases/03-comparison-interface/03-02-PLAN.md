---
phase: 03-comparison-interface
plan: 02
type: execute
---

<objective>
Build side-by-side pair comparison view for two antibiotics.

Purpose: Create classic two-column comparison interface for head-to-head antibiotic comparison, showing properties in parallel with visual highlighting for differences.
Output: Functional pair comparison component that clearly shows how two antibiotics differ.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-comparison-interface/03-CONTEXT.md

# Prior summaries
@.planning/phases/01-foundation-data-architecture/01-02-SUMMARY.md
@.planning/phases/02-visual-network-exploration/02-03-SUMMARY.md
@.planning/phases/03-comparison-interface/03-01-SUMMARY.md

# Key files
@src/types/medical.types.ts
@src/types/comparison.types.ts
@src/data/EnhancedAntibioticData.ts

**Tech stack available:** React 18, TypeScript, Tailwind CSS for styling
**Established patterns:** React.FC with props interfaces, PascalCase components, 2-space indentation
**Constraining decisions:**
- Phase 03-CONTEXT: Clear visual differentiation is essential - differences must be obvious at a glance
- Phase 03-CONTEXT: Mechanism of action is essential property for comparison
- Phase 02-03: Medical UX principle - clarity over complexity
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build two-column layout component</name>
  <files>src/components/comparison/PairComparison.tsx</files>
  <action>Create PairComparison component with side-by-side two-column layout. Props: leftAntibiotic (Antibiotic), rightAntibiotic (Antibiotic), onClose (callback). Use CSS Grid with two equal columns (grid-cols-2). Each column: antibiotic name header (h2), antibiotic class subtitle (h3), content area for properties. Use Tailwind classes: border, padding, rounded corners. Add close button in top-right corner. Use React.FC pattern. Import Antibiotic from src/types. If either prop is null, show "Select two antibiotics to compare" message. Follow project conventions: PascalCase file, double quotes, 2-space indent.</action>
  <verify>Component renders with two antibiotics side-by-side, layout responsive, close button functional</verify>
  <done>Two-column layout renders correctly, header shows antibiotic names and classes, structure ready for property rows</done>
</task>

<task type="auto">
  <name>Task 2: Create property comparison table</name>
  <files>src/components/comparison/PropertyComparisonRow.tsx</files>
  <action>Create PropertyComparisonRow component for displaying one property across both antibiotics. Props: propertyName (string), leftValue (string | string[]), rightValue (string | string[]), isDifferent (boolean). Layout: three columns - property name (left, 20% width), left value (center, 40%), right value (right, 40%). Handle array values by joining with commas. Use Tailwind for styling: border-b for row separation, py-2 for padding. Apply conditional background color when isDifferent=true (bg-yellow-50 for highlighting differences). Handle Northwestern spectrum as special case - display as "Coverage: [count] pathogens" for brevity. Export as React.FC. Import in PairComparison and render rows for: mechanism of action, antibiotic class, formulations, route of administration, Northwestern coverage summary.</action>
  <verify>Property rows render in PairComparison, array values display correctly, different values highlighted</verify>
  <done>PropertyComparisonRow displays properties correctly, handles arrays and objects, visual highlighting for differences works</done>
</task>

<task type="auto">
  <name>Task 3: Add visual diff highlighting</name>
  <files>src/components/comparison/PairComparison.tsx</files>
  <action>Enhance PairComparison with automatic difference detection. Create helper function compareProperties(left: Antibiotic, right: Antibiotic) that returns PropertyDiff[] with property name, values, and isDifferent boolean. Compare: mechanismOfAction (string equality), antibioticClass (string equality), formulations (array equality - use JSON.stringify or array comparison), routeOfAdministration (string equality), northwesternSpectrum (compare coverage levels for each segment, consider different if any segment differs). Iterate comparison results to render PropertyComparisonRow components with isDifferent prop. Add visual legend at top: small colored boxes showing "Same" (gray) and "Different" (yellow) with labels. Use useMemo to memoize compareProperties results (only recalculate when antibiotics change).</action>
  <verify>Differences automatically detected and highlighted, legend shows color meanings, comparison is performant</verify>
  <done>Visual diff highlighting functional, properties compared accurately, legend clear, no unnecessary re-renders</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without new errors
- [ ] PairComparison renders with two real antibiotics
- [ ] Property rows display all key properties
- [ ] Differences highlighted with yellow background
- [ ] Legend explains highlighting colors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Side-by-side layout clear and readable
- Visual differences immediately obvious
- Mechanism of action displayed prominently
</success_criteria>

<output>
After completion, create `.planning/phases/03-comparison-interface/03-02-SUMMARY.md`:

# Phase 3 Plan 2: Side-by-Side Pair Comparison Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- [File list with descriptions]

## Decisions Made

[Decisions or "None"]

## Issues Encountered

[Issues or "None"]

## Next Step

Ready for 03-03-PLAN.md (Reference Comparison Foundation - PRIMARY mode)
</output>
