---
phase: 03-comparison-interface
plan: 05
type: execute
---

<objective>
Build multi-item grid comparison for 3+ antibiotics simultaneously.

Purpose: Enable comparison of multiple antibiotics in a grid layout for overview and pattern recognition across several options.
Output: Responsive grid comparison component supporting 3-6 antibiotics with compact property display.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-comparison-interface/03-CONTEXT.md

# Prior summaries
@.planning/phases/03-comparison-interface/03-01-SUMMARY.md
@.planning/phases/03-comparison-interface/03-02-SUMMARY.md

# Key files
@src/types/medical.types.ts
@src/types/comparison.types.ts

**Tech stack available:** React 18, TypeScript, Tailwind CSS with responsive breakpoints
**Established patterns:** React.FC, useMemo, responsive design with Tailwind grid
**Constraining decisions:**
- Phase 03-CONTEXT: Visual differentiation at a glance is essential
- Phase 02-03: Progressive disclosure - layer information for reduced cognitive load
- CONVENTIONS.md: Responsive patterns with Tailwind breakpoints
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build responsive grid layout</name>
  <files>src/components/comparison/GridComparison.tsx</files>
  <action>Create GridComparison component with responsive grid. Props: antibiotics (Antibiotic[], max 6 items), onRemove (callback with antibiotic id), onClose (callback). Use CSS Grid with responsive breakpoints: mobile (1 column), tablet (2 columns at md breakpoint), desktop (3 columns at lg breakpoint). Tailwind classes: "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4". Each grid item is a card showing one antibiotic. Add header with "Comparing [N] Antibiotics" title and close button. If antibiotics.length < 3, show message "Select at least 3 antibiotics for grid comparison". If > 6, show warning "Maximum 6 antibiotics for grid view". Use React.FC pattern. Map over antibiotics array to render cards (placeholder divs for now with antibiotic name).</action>
  <verify>Grid layout responsive, switches columns at breakpoints, displays 3-6 antibiotics correctly, error messages for invalid counts</verify>
  <done>GridComparison renders with responsive grid, breakpoints work, min/max validation functional</done>
</task>

<task type="auto">
  <name>Task 2: Create compact property cards</name>
  <files>src/components/comparison/CompactAntibioticCard.tsx</files>
  <action>Create CompactAntibioticCard component for grid items. Props: antibiotic (Antibiotic), onRemove (callback), isHighlighted (boolean). Card structure: header (antibiotic name h3, remove button X), property list (mechanism, class, formulations as comma-separated, route). Use compact vertical layout with smaller text (text-sm). Each property as single line with label + value: "Mechanism: [value]", "Class: [value]", "Forms: [values]", "Route: [value]". Add Northwestern coverage visualization: small inline bar chart or colored dots showing coverage across 8 segments (8 small circles colored by coverage level - gray=none, yellow=moderate, green=good). Apply border-2 border-blue-500 if isHighlighted=true. Use Tailwind: card with border, shadow-sm, padding (p-4), rounded. Export as React.FC. Integrate into GridComparison to replace placeholder divs.</action>
  <verify>Compact cards display in grid, all properties visible, Northwestern coverage visualization renders, remove buttons work</verify>
  <done>CompactAntibioticCard complete, integrated in GridComparison, information dense but readable, coverage visualization clear</done>
</task>

<task type="auto">
  <name>Task 3: Add responsive breakpoints and mobile optimization</name>
  <files>src/components/comparison/GridComparison.tsx, src/components/comparison/CompactAntibioticCard.tsx</files>
  <action>Enhance GridComparison and CompactAntibioticCard for mobile. In GridComparison, add useResponsive hook (from existing src/hooks/useResponsive.ts) to detect mobile viewport. On mobile: single column, larger touch targets for remove buttons, simplified header. In CompactAntibioticCard, conditionally render on mobile: hide Northwestern coverage visualization (too small), abbreviate property labels ("Mech:" instead of "Mechanism:"), larger text (text-base instead of text-sm on mobile). Add Tailwind responsive classes: "text-sm lg:text-base", "hidden md:block" for coverage viz. Test grid with 3, 4, 5, and 6 antibiotics to ensure responsive behavior. Add padding adjustments: "p-2 md:p-4" for cards.</action>
  <verify>Grid works on mobile (single column), tablet (2 columns), desktop (3 columns), touch targets adequate, information readable at all sizes</verify>
  <done>Responsive breakpoints functional, mobile optimization complete, grid layout adapts smoothly across viewport sizes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without new errors
- [ ] GridComparison renders 3-6 antibiotics in responsive grid
- [ ] Cards display compactly with all key properties
- [ ] Northwestern coverage visualization works
- [ ] Mobile layout uses single column
- [ ] Tablet/desktop layouts use 2/3 columns
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Grid comparison supports 3-6 antibiotics
- Responsive design works across mobile/tablet/desktop
- Compact cards readable and informative
- Northwestern coverage visible (except mobile)
</success_criteria>

<output>
After completion, create `.planning/phases/03-comparison-interface/03-05-SUMMARY.md`:

# Phase 3 Plan 5: Multi-Item Grid Comparison Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- [File list]

## Decisions Made

[Decisions or "None"]

## Issues Encountered

[Issues or "None"]

## Next Step

Ready for 03-06-PLAN.md (Visual Differentiation System)
</output>
