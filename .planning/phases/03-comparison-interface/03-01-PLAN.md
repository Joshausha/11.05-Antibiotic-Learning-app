---
phase: 03-comparison-interface
plan: 01
type: execute
---

<objective>
Create comparison selection system and state management foundation.

Purpose: Establish TypeScript interfaces and selection UI that enable users to choose which antibiotics to compare across all three comparison modes (side-by-side, reference, multi-item grid).
Output: Working antibiotic selection component with type-safe state management.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-comparison-interface/03-CONTEXT.md

# Prior summaries (filtered by subsystem relevance)
@.planning/phases/01-foundation-data-architecture/01-02-SUMMARY.md
@.planning/phases/02-visual-network-exploration/02-02-SUMMARY.md

# Key files for reference
@src/types/medical.types.ts
@src/types/network-ui.types.ts
@src/data/EnhancedAntibioticData.ts

**Tech stack available:** React 18, TypeScript 5.9.3 (strict mode), existing medical.types.ts interfaces
**Established patterns:** Centralized state interfaces, barrel exports for types, JSDoc documentation
**Constraining decisions:**
- Phase 01-02: Export all types via barrel file pattern (src/types/index.ts)
- Phase 02-02: Use single object for related state (NetworkFilters pattern) to prevent state combination bugs
- Phase 03-CONTEXT: Reference comparison is PRIMARY mode - optimize interface for this workflow
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define comparison state TypeScript interfaces</name>
  <files>src/types/comparison.types.ts</files>
  <action>Create comparison.types.ts with interfaces for ComparisonState (selectedAntibiotics: string[], referenceAntibiotic: string | null, comparisonMode: 'pair' | 'reference' | 'grid'), SelectedAntibioticDetails (id, name, class, mechanism, formulation, coverage spectrum), and ComparisonDiff (property name, values array, isDifferent boolean). Follow medical.types.ts pattern: strict TypeScript with union types, JSDoc documentation with medical examples, export interfaces. Add exports to src/types/index.ts barrel file. Use existing Antibiotic interface from medical.types.ts as foundation.</action>
  <verify>TypeScript compiles without errors, interfaces exported from src/types/index.ts</verify>
  <done>ComparisonState, SelectedAntibioticDetails, and ComparisonDiff interfaces defined with proper types and JSDoc, exported via barrel file</done>
</task>

<task type="auto">
  <name>Task 2: Create antibiotic multi-select component</name>
  <files>src/components/comparison/AntibioticSelector.tsx</files>
  <action>Create AntibioticSelector component with checkbox list for antibiotic selection. Props: antibiotics (Antibiotic[]), selectedIds (string[]), onSelectionChange (callback), maxSelections (default 5). Display antibiotics grouped by class (beta-lactams, macrolides, etc.) with expand/collapse. Show antibiotic name and class. Use React.FC pattern per CONVENTIONS.md. Use useState for expand/collapse state. Implement checkbox onChange handler calling onSelectionChange with updated array. Disable checkboxes when maxSelections reached (except unchecking). Import Antibiotic type from src/types/index.ts. Follow PascalCase naming, 2-space indentation, double quotes.</action>
  <verify>Component renders with EnhancedAntibioticData, selection changes trigger callback, max selections enforced</verify>
  <done>AntibioticSelector component functional, selection state updates correctly, enforces max limit, groups by antibiotic class</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript compiles without new errors
- [ ] All interfaces exported from barrel file
- [ ] AntibioticSelector renders with real antibiotic data
- [ ] Selection state updates trigger callbacks correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors introduced
- ComparisonState interface ready for all three comparison modes
- AntibioticSelector working with real medical data
</success_criteria>

<output>
After completion, create `.planning/phases/03-comparison-interface/03-01-SUMMARY.md`:

# Phase 3 Plan 1: Comparison Selection & State Summary

**[Substantive one-liner - what shipped, not "phase complete"]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description
- `path/to/another.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 03-02-PLAN.md (Side-by-Side Pair Comparison)
</output>
