---
phase: 07-performance-polish
plan: 03
type: execute
---

<objective>
Fix TypeScript errors in Northwestern data schemas and style files - resolve ~88 errors in core data infrastructure.

Purpose: Northwestern visualization is the primary visual interface. Type safety in schema/style files ensures consistent rendering.
Output: Zero TypeScript errors in Northwestern schema and style files.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-polish/07-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

**Current Errors:**
- src/data/NorthwesternAntibioticSchema.ts: 32 errors
- src/styles/NorthwesternVisualStates.ts: 21 errors
- src/styles/NorthwesternColors.ts: 14 errors
- src/config/northwesternIntegrationConfig.ts: 8 errors

**Total:** ~75 errors in Northwestern infrastructure files

**Key Types:**
- NorthwesternSpectrum: 8-segment coverage model
- CoverageLevel: 0 | 1 | 2 (not covered, moderate, good)
- SegmentDefinition: segment metadata
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NorthwesternAntibioticSchema.ts errors</name>
  <files>src/data/NorthwesternAntibioticSchema.ts, src/types/medical.types.ts</files>
  <action>
1. Run `npx tsc --noEmit 2>&1 | grep "NorthwesternAntibioticSchema.ts"` to see errors
2. Common fixes:
   - CoverageLevel should be 0 | 1 | 2, not generic number
   - Use `as CoverageLevel` assertions if needed
   - Ensure all 8 segments are defined for each antibiotic
   - Fix any property name mismatches with NorthwesternSpectrum interface
3. The schema defines the canonical antibiotic coverage data - type safety is critical

WHY: This is THE medical data source. Type errors here could mean coverage data is malformed.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -c "NorthwesternAntibioticSchema.ts" should be 0</verify>
  <done>Zero TypeScript errors in NorthwesternAntibioticSchema.ts</done>
</task>

<task type="auto">
  <name>Task 2: Fix Northwestern style and config files</name>
  <files>src/styles/NorthwesternVisualStates.ts, src/styles/NorthwesternColors.ts, src/config/northwesternIntegrationConfig.ts</files>
  <action>
1. Fix NorthwesternVisualStates.ts (21 errors):
   - Likely index signature issues or missing type annotations
   - Add proper typing for visual state objects
   - Use Record<string, T> for dynamic keys if needed

2. Fix NorthwesternColors.ts (14 errors):
   - Color constants should have proper typing
   - Use `as const` for color literal strings
   - Ensure coverage level colors map correctly to CoverageLevel type

3. Fix northwesternIntegrationConfig.ts (8 errors):
   - Configuration objects need proper interfaces
   - Feature flags should be boolean typed
   - Integration settings should match expected shapes

WHY: Visual consistency depends on properly typed style and config. These support the entire Northwestern visualization system.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -E "(NorthwesternVisualStates|NorthwesternColors|northwesternIntegrationConfig)" | wc -l should be 0</verify>
  <done>Zero TypeScript errors in all Northwestern style and config files</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit 2>&1 | grep "Northwestern"` shows significantly fewer errors
- [ ] Core data schema properly typed
- [ ] Visual state management properly typed
- [ ] Color and config files properly typed
</verification>

<success_criteria>

- Zero TypeScript errors in Northwestern infrastructure (~75 errors eliminated)
- CoverageLevel type strictly enforced (0 | 1 | 2)
- Northwestern visualization foundation type-safe
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-polish/07-03-SUMMARY.md`
</output>
