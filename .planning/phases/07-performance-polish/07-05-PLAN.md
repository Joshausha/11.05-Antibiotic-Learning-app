---
phase: 07-performance-polish
plan: 05
type: execute
---

<objective>
Fix TypeScript errors in utility functions and remaining components - resolve remaining errors to achieve clean TypeScript compilation.

Purpose: Complete TypeScript cleanup to enable removal of TSC_COMPILE_ON_ERROR workaround.
Output: TypeScript error count reduced to near-zero, enabling strict type checking.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-polish/07-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

**Remaining Error Categories (after plans 02-04):**
- Utility files: pathogenSimilarity.ts, spacedRepetitionManager.ts, transformRboData.ts
- Clinical Decision components: ClinicalDecisionEngine.ts, ClinicalDecisionTree.tsx, etc.
- Comparison components: ComparisonModeContainer.tsx
- Other scattered errors

**Note:** After plans 02-04 complete, run `npx tsc --noEmit 2>&1 | wc -l` to see remaining count.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix utility function type errors</name>
  <files>src/utils/pathogenSimilarity.ts, src/utils/spacedRepetitionManager.ts, src/utils/transformRboData.ts, src/utils/northwesternPerformanceOptimizer.ts</files>
  <action>
1. Fix pathogenSimilarity.ts:
   - SimplePathogen type mismatch (gramStain string vs union)
   - Add proper type imports from canonical sources
   - Use type guards if needed for runtime safety

2. Fix spacedRepetitionManager.ts:
   - RecommendedQuestion type predicate issues
   - Priority type mismatches ('high' | 'medium' | 'normal')
   - Fix null handling with proper type narrowing

3. Fix transformRboData.ts:
   - Return type mismatches
   - TransformedCondition vs RboConditionData incompatibility
   - May need interface alignment or separate interfaces

4. Fix northwesternPerformanceOptimizer.ts (9 errors):
   - Performance-related typing
   - Timing and metric types

WHY: Utility functions are used across the app. Type errors here propagate everywhere.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep "src/utils" | wc -l should be significantly reduced</verify>
  <done>Major utility files have zero TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Fix Clinical Decision component types</name>
  <files>src/components/ClinicalDecision/ClinicalDecisionEngine.ts, src/components/ClinicalDecision/ClinicalDecisionTree.tsx, src/components/ClinicalDecision/ClinicalInputPanel.tsx, src/components/ClinicalDecision/GuidelineComparisonPanel.tsx, src/components/ClinicalDecision/DecisionPathwayRenderer.tsx</files>
  <action>
1. ClinicalDecisionEngine.ts:
   - AntibioticRecommendation export issue
   - Type conversion errors
   - Fix module exports in clinical-decision.types.ts if needed

2. ClinicalDecisionTree.tsx:
   - null vs undefined type issues
   - ProcessedNode vs TreeNode property mismatches

3. ClinicalInputPanel.tsx:
   - string | number vs number type issues
   - SeverityType handling

4. GuidelineComparisonPanel.tsx:
   - Property access on wrong types
   - Array type conversions

5. DecisionPathwayRenderer.tsx:
   - D3 implicit any issues
   - Add proper D3 types or explicit annotations

WHY: Clinical decision support is a key feature. Type safety ensures correct recommendation logic.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep "ClinicalDecision" | wc -l should be 0 or near 0</verify>
  <done>Clinical Decision components have zero TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 3: Fix remaining scattered errors</name>
  <files>Various - based on remaining errors after tasks 1-2</files>
  <action>
1. Run `npx tsc --noEmit 2>&1 | grep "error TS"` to identify remaining errors
2. Group by file and fix systematically
3. Focus on:
   - Comparison mode container (9 errors)
   - Any animation/demo files still with errors
   - App.tsx @ts-ignore comments (may need to keep some temporarily)

4. For remaining @ts-ignore in App.tsx:
   - If component is JavaScript and hard to type, document why @ts-ignore is used
   - Add TODO comments for future migration

5. Target: <20 remaining errors (some may be acceptable for learning project)

WHY: Get as close to zero as practical. Some @ts-ignore may be acceptable if documented.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep "error TS" | wc -l should be <20</verify>
  <done>TypeScript errors reduced to <20 (documented exceptions only)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` < 20
- [ ] Utility functions properly typed
- [ ] Clinical Decision components properly typed
- [ ] Remaining errors are documented with reasoning
</verification>

<success_criteria>

- TypeScript errors reduced to <20 (from 655)
- Remaining errors are documented exceptions
- Ready to test removal of TSC_COMPILE_ON_ERROR
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-polish/07-05-SUMMARY.md`
</output>
