---
phase: 07-performance-polish
plan: 04
type: execute
---

<objective>
Fix TypeScript errors in primary visualization components - resolve ~120 errors in key UI components.

Purpose: Type-safe components prevent runtime errors and make the codebase easier to understand and modify.
Output: Zero TypeScript errors in core visualization and explorer components.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-polish/07-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

**Current Errors by Component:**
- src/components/visualizations/OverviewDashboard.tsx: 23 errors
- src/components/OptimizedNorthwesternIntegration.tsx: 21 errors
- src/components/SimplePathogenExplorer.tsx: 19 errors
- src/components/NorthwesternSpatialLayout.tsx: 18 errors
- src/components/PathogenExplorer.tsx: 16 errors
- src/components/NetworkVisualizationD3.tsx: 16 errors
- src/components/VisualizationsTab.tsx: 12 errors
- src/components/AntibioticExplorer.tsx: 11 errors

**Total:** ~136 errors in visualization components

**Common Error Patterns:**
- Antibiotic type mismatches (different Antibiotic interfaces)
- Event handler type mismatches
- Optional vs required props
- Index signature issues
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix primary explorer component types</name>
  <files>src/components/AntibioticExplorer.tsx, src/components/PathogenExplorer.tsx, src/components/SimplePathogenExplorer.tsx</files>
  <action>
1. Fix AntibioticExplorer.tsx (11 errors):
   - Ensure using correct Antibiotic type from medical.types.ts (not local redefinition)
   - Fix event handler signatures to match expected types
   - Add missing props to component calls
   - Use proper index types for NorthwesternSpectrum access

2. Fix PathogenExplorer.tsx (16 errors):
   - Same pattern - use canonical types
   - Fix prop types passed to child components

3. Fix SimplePathogenExplorer.tsx (19 errors):
   - This is likely a simplified version - ensure types match
   - May need to use same canonical interfaces

WHY: Explorer components are the main entry points for users. Type safety here catches bugs before runtime.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -E "(AntibioticExplorer|PathogenExplorer|SimplePathogenExplorer)" | wc -l should be 0</verify>
  <done>Zero TypeScript errors in explorer components (~46 errors fixed)</done>
</task>

<task type="auto">
  <name>Task 2: Fix Northwestern visualization component types</name>
  <files>src/components/NorthwesternSpatialLayout.tsx, src/components/OptimizedNorthwesternIntegration.tsx, src/components/NetworkVisualizationD3.tsx</files>
  <action>
1. Fix NorthwesternSpatialLayout.tsx (18 errors):
   - Layout component for spatial antibiotic visualization
   - Ensure segment types match NorthwesternSpectrum
   - Fix any D3-related typing issues

2. Fix OptimizedNorthwesternIntegration.tsx (21 errors):
   - Integration layer between components
   - Props passed between components need consistent types
   - May have duplicate type definitions to remove

3. Fix NetworkVisualizationD3.tsx (16 errors):
   - D3 visualization has implicit any issues
   - Add @types/d3 type definitions if missing
   - Use proper D3 types for selections, simulations

WHY: These are the core visualization renderers. Type safety ensures data flows correctly through the visual pipeline.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -E "(NorthwesternSpatialLayout|OptimizedNorthwestern|NetworkVisualizationD3)" | wc -l should be 0</verify>
  <done>Zero TypeScript errors in visualization components (~55 errors fixed)</done>
</task>

<task type="auto">
  <name>Task 3: Fix dashboard and tab component types</name>
  <files>src/components/visualizations/OverviewDashboard.tsx, src/components/VisualizationsTab.tsx</files>
  <action>
1. Fix OverviewDashboard.tsx (23 errors):
   - Dashboard aggregates multiple visualizations
   - Fix prop types passed to child components
   - Ensure data types match expected interfaces

2. Fix VisualizationsTab.tsx (12 errors):
   - Tab container for visualization modes
   - Fix any mode switching type issues
   - Ensure state types are correct

WHY: Container components coordinate the UI. Type errors here cascade to child components.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -E "(OverviewDashboard|VisualizationsTab)" | wc -l should be 0</verify>
  <done>Zero TypeScript errors in dashboard and tab components (~35 errors fixed)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit 2>&1 | grep "src/components"` shows significantly fewer errors
- [ ] Explorer components type-safe
- [ ] Visualization components type-safe
- [ ] Dashboard/tab components type-safe
- [ ] No local Antibiotic type redefinitions conflicting with canonical type
</verification>

<success_criteria>

- Zero TypeScript errors in major visualization components (~136 errors eliminated)
- Components use canonical types from medical.types.ts
- Event handler types consistent across components
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-polish/07-04-SUMMARY.md`
</output>
