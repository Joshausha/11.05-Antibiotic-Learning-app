---
phase: 07-performance-polish
plan: 06
type: execute
---

<objective>
Remove TSC_COMPILE_ON_ERROR workaround and verify clean TypeScript build.

Purpose: Enable strict TypeScript checking as a safety net for future development.
Output: Clean build without error suppression, TypeScript catches bugs at compile time.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-polish/07-CONTEXT.md

**Current State (before plans 01-05):**
- package.json line 9: `"build": "GENERATE_SOURCEMAP=false TSC_COMPILE_ON_ERROR=true node scripts/build.js"`
- TSC_COMPILE_ON_ERROR=true allows build despite 655 TypeScript errors
- This was intentional for learning but prevents type safety benefits

**After Plans 01-05:**
- TypeScript errors should be <20
- Ready to attempt strict build
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove TSC_COMPILE_ON_ERROR from build script</name>
  <files>package.json</files>
  <action>
1. Verify TypeScript errors are low: `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l`
2. If errors > 20, STOP and report - need more cleanup first
3. If errors < 20, update package.json build script:
   - FROM: `"build": "GENERATE_SOURCEMAP=false TSC_COMPILE_ON_ERROR=true node scripts/build.js"`
   - TO: `"build": "GENERATE_SOURCEMAP=false node scripts/build.js"`
4. Test build: `npm run build`
5. If build fails, identify blocking errors and fix them directly

WHY: TSC_COMPILE_ON_ERROR was a crutch. Removing it enables TypeScript to protect the codebase.
  </action>
  <verify>npm run build should complete successfully without TSC_COMPILE_ON_ERROR</verify>
  <done>Build succeeds with strict TypeScript checking enabled</done>
</task>

<task type="auto">
  <name>Task 2: Verify all core functionality still works</name>
  <files>None - verification only</files>
  <action>
1. Run full test suite: `npm test -- --watchAll=false`
2. Verify tests pass at same rate as before (no regression)
3. Run dev server: `npm start` - verify app loads
4. Test key flows manually if needed:
   - Northwestern pie chart renders
   - Network visualization loads
   - Quiz system works
5. Document any issues found

WHY: Strict typing should not change runtime behavior, but verify nothing broke.
  </action>
  <verify>npm test passes at same or better rate than before TypeScript fixes</verify>
  <done>All tests pass, dev server runs, app functions correctly</done>
</task>

<task type="auto">
  <name>Task 3: Document remaining TypeScript exceptions</name>
  <files>.planning/phases/07-performance-polish/TYPESCRIPT_EXCEPTIONS.md</files>
  <action>
1. If any @ts-ignore or @ts-nocheck remain, document them:
   - File location
   - Why it's needed
   - What would fix it (for future reference)
2. Create TYPESCRIPT_EXCEPTIONS.md if exceptions exist
3. If zero exceptions, skip this task and note in summary

Example content:
```markdown
# TypeScript Exceptions

## src/App.tsx
- Lines 83, 98: @ts-ignore for JavaScript components (Header, HomeTab)
- Reason: Components not yet migrated to TypeScript
- Fix: Migrate components to TypeScript
```

WHY: Future developers (including Josh) need to know why exceptions exist.
  </action>
  <verify>All remaining @ts-ignore comments are documented with reasoning</verify>
  <done>TypeScript exceptions documented OR no exceptions remain</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without TSC_COMPILE_ON_ERROR
- [ ] `npm test -- --watchAll=false` passes at expected rate
- [ ] `npm start` launches dev server without errors
- [ ] Any remaining TypeScript exceptions are documented
</verification>

<success_criteria>

- Clean TypeScript build without error suppression
- Runtime behavior unchanged
- TypeScript now provides meaningful compile-time safety
- Zero TypeScript errors achieved OR exceptions documented
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-polish/07-06-SUMMARY.md`
</output>
