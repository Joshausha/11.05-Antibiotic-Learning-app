---
phase: 07-performance-polish
plan: 02
type: execute
---

<objective>
Fix TypeScript errors in quiz question data files - resolve 158 errors in quizQuestions.ts and quizQuestionsWithDifficulty.ts.

Purpose: Quiz data is foundational medical content. Type safety ensures questions have correct structure and required fields.
Output: Zero TypeScript errors in quiz data files with proper type enforcement.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-polish/07-CONTEXT.md
@src/types/medical.types.ts

**Current Errors (158 total):**
- src/data/quizQuestions.ts: 79 errors
- src/data/quizQuestionsWithDifficulty.ts: 79 errors

**Type Definition** (from Phase 1-02):
The QuizQuestion interface in medical.types.ts defines required fields including medicalAccuracyVerified.

**Error Pattern Analysis:**
Most errors are likely:
1. Missing required fields (medicalAccuracyVerified, lastUpdated)
2. String literals not matching union types
3. Optional fields typed incorrectly
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze and fix quizQuestions.ts type errors</name>
  <files>src/data/quizQuestions.ts, src/types/medical.types.ts</files>
  <action>
1. Run `npx tsc --noEmit 2>&1 | grep "quizQuestions.ts" | head -20` to see specific errors
2. Common fixes:
   - Add `medicalAccuracyVerified: false` to all questions (validation tests will flag for review)
   - Add `lastUpdated: '2026-01-07'` to questions
   - Fix difficulty literals to match `'beginner' | 'intermediate' | 'advanced'`
   - Fix category strings to match expected union type
3. Use `as const` assertions for literal types if needed
4. If QuizQuestion interface is too strict for current data, consider:
   - Making some fields optional in interface (document why)
   - OR fixing all data to match interface (preferred)

WHY: Type safety in medical content prevents bugs like missing explanations or malformed questions.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -c "quizQuestions.ts" should be 0 (not quizQuestionsWithDifficulty)</verify>
  <done>Zero TypeScript errors in src/data/quizQuestions.ts</done>
</task>

<task type="auto">
  <name>Task 2: Fix quizQuestionsWithDifficulty.ts type errors</name>
  <files>src/data/quizQuestionsWithDifficulty.ts</files>
  <action>
1. Apply same fixes as quizQuestions.ts
2. This file likely has identical structure - same error patterns
3. If this file is a duplicate/variant of quizQuestions.ts, consider:
   - Consolidating to single source of truth (per Phase 1-01 recommendations)
   - OR ensuring both files have consistent typing
4. Add medicalAccuracyVerified: false placeholder to all questions

WHY: Consistency between quiz data files prevents confusion about which is canonical.
  </action>
  <verify>npx tsc --noEmit 2>&1 | grep -c "quizQuestionsWithDifficulty.ts" should be 0</verify>
  <done>Zero TypeScript errors in src/data/quizQuestionsWithDifficulty.ts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit 2>&1 | grep "quizQuestion"` returns no errors
- [ ] Quiz data files have consistent structure
- [ ] All questions have medicalAccuracyVerified field (even if false)
- [ ] npm test passes for quiz-related tests
</verification>

<success_criteria>

- Zero TypeScript errors in quiz data files (158 errors eliminated)
- Quiz data properly typed with QuizQuestion interface
- Medical accuracy verification field present on all questions
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-polish/07-02-SUMMARY.md`
</output>
