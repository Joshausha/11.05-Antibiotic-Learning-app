---
phase: 07-performance-polish
plan: 09
type: execute
---

<objective>
Increase test coverage for core functionality - focus on visualization components, hooks, and user interactions.

Purpose: Confidence that changes don't break core functionality. Tests should catch bugs before they reach the browser.
Output: 80%+ test coverage on core components with meaningful assertions.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-polish/07-CONTEXT.md
@.planning/codebase/TESTING.md

**From 07-CONTEXT.md:**
"Comprehensive test coverage - Confidence that changes don't break core functionality. Tests should catch bugs before they reach the browser."
"80%+ test coverage focused on core functionality, not chasing 100% on edge cases"

**Current Test Status (from TESTING.md):**
- Individual test pass rate: 98%
- Test suite pass rate: 87%
- Test infrastructure: Jest 27.4.3 + React Testing Library
- Coverage command: `npm run test:coverage`

**Core Components to Test (priority order):**
1. Northwestern visualization (NorthwesternPieChart, NorthwesternSpatialLayout)
2. Quiz system (QuizSession, QuizQuestion, QuizExplanation)
3. Comparison mode (ComparisonModeContainer, PairComparison, ReferenceComparison)
4. Custom hooks (useNetworkLayoutSimulation, useVisualizationState, useNorthwesternErrorRecovery)
5. Network visualization (D3NetworkGraph, PathogenNetworkVisualization)

**Existing Test Coverage Gaps (from CONCERNS.md):**
- Experimental visualizations not tested
- Multiple Northwestern variants coexist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add/expand Northwestern visualization tests</name>
  <files>src/components/__tests__/NorthwesternPieChart.test.tsx, src/components/__tests__/NorthwesternSpatialLayout.test.tsx</files>
  <action>
1. Test NorthwesternPieChart component:
   - Renders without crashing with valid antibiotic data
   - Displays correct number of segments (8)
   - Handles null/undefined antibiotic gracefully
   - Coverage colors match coverage levels (gray=0, yellow=1, green=2)
   - Click handlers fire with correct segment data

2. Test NorthwesternSpatialLayout component:
   - Renders multiple antibiotics in spatial layout
   - Drug class grouping works correctly
   - Selection state updates on click
   - Hover states display tooltip content

3. Test edge cases:
   - Empty antibiotic list
   - Single antibiotic
   - Missing Northwestern coverage data

WHY: Northwestern visualization is the primary learning interface. Bugs here break the core experience.
  </action>
  <verify>npm test -- --testPathPattern="Northwestern" --watchAll=false --coverage shows >80% for Northwestern components</verify>
  <done>Northwestern visualization tests provide >80% coverage with meaningful assertions</done>
</task>

<task type="auto">
  <name>Task 2: Add quiz system integration tests</name>
  <files>src/components/__tests__/QuizSession.test.tsx, src/components/__tests__/QuizQuestion.test.tsx, src/components/__tests__/QuizExplanation.test.tsx</files>
  <action>
1. Test QuizSession flow:
   - Starts with question 1
   - Progresses through questions on answer
   - Tracks correct/incorrect counts
   - Shows results at end
   - Retry resets state

2. Test QuizQuestion component:
   - Renders question text and options
   - Highlights selected answer
   - Shows correct/incorrect state after answer
   - Disables options after answering

3. Test QuizExplanation component:
   - Shows explanation after answer
   - Displays correct answer indicator
   - Shows related antibiotic information
   - Next button works correctly

4. Test user interactions:
   - Clicking option selects it
   - Cannot change answer after submission
   - Keyboard navigation works (if implemented)

WHY: Quiz is the retention mechanism. Broken quiz flow prevents learning.
  </action>
  <verify>npm test -- --testPathPattern="Quiz" --watchAll=false --coverage shows >80% for Quiz components</verify>
  <done>Quiz system tests cover full user flow with >80% coverage</done>
</task>

<task type="auto">
  <name>Task 3: Add custom hooks tests</name>
  <files>src/hooks/__tests__/useVisualizationState.test.ts, src/hooks/__tests__/useNetworkLayoutSimulation.test.ts, src/hooks/__tests__/useNorthwesternErrorRecovery.test.ts</files>
  <action>
1. Test useVisualizationState hook:
   - Initial state is correct
   - State updates work as expected
   - Derived values calculate correctly
   - Selection state syncs properly

2. Test useNetworkLayoutSimulation hook:
   - Simulation initializes with nodes/edges
   - Force simulation produces stable layout
   - Cleanup stops simulation on unmount
   - Handles empty node list

3. Test useNorthwesternErrorRecovery hook:
   - Error state initializes as null
   - setError updates state
   - Recovery function clears error
   - Retry logic works

4. Use @testing-library/react-hooks or renderHook from @testing-library/react:
```typescript
import { renderHook, act } from '@testing-library/react';

test('useVisualizationState updates selection', () => {
  const { result } = renderHook(() => useVisualizationState());
  act(() => {
    result.current.selectAntibiotic('amoxicillin');
  });
  expect(result.current.selectedAntibiotic).toBe('amoxicillin');
});
```

WHY: Hooks contain shared logic. Bugs in hooks cascade to all components using them.
  </action>
  <verify>npm test -- --testPathPattern="hooks" --watchAll=false --coverage shows >80% for custom hooks</verify>
  <done>Custom hooks have comprehensive test coverage >80%</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:coverage` shows overall coverage improvement
- [ ] Northwestern components: >80% coverage
- [ ] Quiz components: >80% coverage
- [ ] Custom hooks: >80% coverage
- [ ] All new tests pass consistently
- [ ] Tests catch real bugs (verify by temporarily breaking code)
</verification>

<success_criteria>

- Test coverage increased to 80%+ for core functionality
- Northwestern visualization thoroughly tested
- Quiz user flow tested end-to-end
- Custom hooks have isolated unit tests
- Tests are meaningful (not just "renders without crashing")
- Running `npm run test:coverage` shows progress toward 80% goal
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-polish/07-09-SUMMARY.md`
</output>
