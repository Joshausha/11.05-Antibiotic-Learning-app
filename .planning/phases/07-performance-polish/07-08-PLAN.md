---
phase: 07-performance-polish
plan: 08
type: execute
---

<objective>
Add medical data accuracy tests - validate antibiotic coverage relationships, pathogen data, and quiz answer correctness.

Purpose: Medical accuracy is the foundation. If the medical content is wrong, nothing else matters. These tests ensure the educational content is trustworthy.
Output: Comprehensive test suite validating medical data integrity and relationships.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-polish/07-CONTEXT.md
@.planning/codebase/TESTING.md
@src/types/medical.types.ts

**From 07-CONTEXT.md:**
"Medical data accuracy tests - Tests verify antibiotic coverage relationships, pathogen data, quiz answers are correct. This is the foundation - if the medical content is wrong, nothing else matters."

**Existing Medical Validation (from Phase 1-03):**
- src/data/__tests__/medicalAccuracyValidation.test.ts exists
- Tests for MRSA/MSSA coverage logic relationship
- Tests for medicalAccuracyVerified field requirement

**Medical Data Files:**
- src/data/EnhancedAntibioticData.ts - Canonical antibiotic source
- src/data/NorthwesternAntibioticSchema.ts - Coverage relationships
- src/data/quizQuestions.ts - Quiz content
- src/data/PathogenRelationshipData.ts - Pathogen relationships

**Key Medical Relationships to Validate:**
- MRSA coverage implies MSSA coverage (resistant strain logic)
- Gram stain consistency (antibiotic coverage matches stated gram categories)
- Quiz answers match actual antibiotic properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Northwestern coverage validation tests</name>
  <files>src/data/__tests__/northwesternCoverageValidation.test.ts</files>
  <action>
1. Create or expand tests for Northwestern 8-segment coverage model:
   - Each antibiotic has valid CoverageLevel (0, 1, or 2) for all 8 segments
   - Coverage levels are medically consistent:
     - If covers gram-positive cocci well (2), should have some coverage of Streptococcus (>=1)
     - If covers MRSA (>=1), must cover MSSA (>=1) - resistant implies sensitive
     - Anaerobic coverage patterns are consistent

2. Test data structure integrity:
   - All antibiotics in EnhancedAntibioticData have NorthwesternSpectrum
   - No undefined or null coverage values
   - Coverage arrays have exactly 8 segments

3. Test cross-reference consistency:
   - Antibiotic names match between data files
   - Class assignments are consistent
   - Formulation data is complete

WHY: Northwestern coverage is THE medical learning tool. Incorrect coverage data teaches wrong information.
  </action>
  <verify>npm test -- --testPathPattern="northwesternCoverageValidation" --watchAll=false passes</verify>
  <done>Northwestern coverage validation tests cover all 8 segments and key medical relationships</done>
</task>

<task type="auto">
  <name>Task 2: Add quiz answer accuracy tests</name>
  <files>src/data/__tests__/quizAnswerAccuracy.test.ts</files>
  <action>
1. Create tests that verify quiz answers against actual antibiotic data:
   - If quiz asks "Does Amoxicillin cover Pseudomonas?", verify answer matches EnhancedAntibioticData
   - If quiz asks about mechanism, verify against antibiotic's mechanism field
   - If quiz asks about class, verify against antibiotic's class field

2. Test question structure integrity:
   - All questions have exactly one correct answer
   - Correct answer index is within options array bounds
   - Explanation references correct antibiotic/pathogen data

3. Test category coverage:
   - Questions exist for each antibiotic class
   - Questions exist for each gram category
   - No orphan questions referencing non-existent antibiotics

WHY: Quiz teaches through testing. Wrong answers teach wrong medicine.
  </action>
  <verify>npm test -- --testPathPattern="quizAnswerAccuracy" --watchAll=false passes</verify>
  <done>Quiz answers validated against canonical medical data sources</done>
</task>

<task type="auto">
  <name>Task 3: Add pathogen data consistency tests</name>
  <files>src/data/__tests__/pathogenDataConsistency.test.ts</files>
  <action>
1. Test pathogen data integrity:
   - All pathogens have valid gramStatus ('positive', 'negative', 'variable', 'none')
   - All pathogens have non-empty name and description
   - Pathogen IDs are unique and follow naming convention

2. Test pathogen-antibiotic relationship consistency:
   - If antibiotic claims coverage of pathogen, pathogen exists in data
   - Coverage relationships are bidirectional (pathogen lists antibiotic, antibiotic lists pathogen)
   - No orphan references

3. Test medical classification accuracy:
   - Gram-positive bacteria are correctly classified (Staph, Strep, Enterococcus)
   - Gram-negative bacteria are correctly classified (E.coli, Pseudomonas, Klebsiella)
   - Atypical organisms are properly categorized

WHY: Pathogen misclassification leads to wrong coverage understanding.
  </action>
  <verify>npm test -- --testPathPattern="pathogenDataConsistency" --watchAll=false passes</verify>
  <done>Pathogen data validated for consistency and medical accuracy</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Northwestern coverage validation tests exist and pass
- [ ] Quiz answer accuracy tests validate against canonical data
- [ ] Pathogen data consistency tests exist and pass
- [ ] All medical data tests can be run with: `npm test -- --testPathPattern="(northwesternCoverage|quizAnswer|pathogenData)" --watchAll=false`
- [ ] No false positives (tests catch actual errors when data is modified incorrectly)
</verification>

<success_criteria>

- Medical data validation tests cover core educational content
- Tests would catch if MRSA/MSSA relationship was violated
- Tests would catch if quiz answer didn't match actual antibiotic data
- Tests would catch if pathogen gram classification was wrong
- Running `npm run test:medical-accuracy` (or similar) validates all medical content
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-polish/07-08-SUMMARY.md`
</output>
