---
phase: 07-performance-polish
plan: 01
type: execute
---

<objective>
Fix broken test infrastructure - resolve test suite failures caused by missing modules and deleted files.

Purpose: Enable reliable test runs as foundation for Phase 7 polish work. Currently 11 test suites fail before any tests run.
Output: All test suites able to execute (may still have individual test failures, but no suite import errors).
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-polish/07-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md

**Current State:**
- 11 test suites failing to execute
- Key failures:
  1. `react-cytoscapejs` package missing - Cytoscape tests can't run
  2. `NorthwesternAnimations.js` deleted - Animation tests reference deleted file
  3. Various test files reference moved/renamed components

**Test Command:**
```bash
npm test -- --watchAll=false
```

**From CONCERNS.md:**
- Cytoscape is an incomplete experiment with D3/simple network fallbacks
- Some tests reference moved/deleted components (.backup files)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Cytoscape test suite and component</name>
  <files>src/components/__tests__/PathogenNetworkVisualizationCytoscape.test.js, src/components/PathogenNetworkVisualizationCytoscape.tsx, package.json</files>
  <action>
1. Delete `src/components/PathogenNetworkVisualizationCytoscape.tsx` - component that imports missing react-cytoscapejs
2. Delete `src/components/__tests__/PathogenNetworkVisualizationCytoscape.test.js` - test for deleted component
3. Remove `react-cytoscapejs` and `cytoscape` from package.json dependencies (both unused)
4. Check for any imports of PathogenNetworkVisualizationCytoscape in other files and remove them

WHY: Cytoscape integration was an incomplete experiment. D3 network visualization works and is the active approach. Keeping broken Cytoscape code causes test failures with no benefit.
  </action>
  <verify>npm test -- --watchAll=false 2>&1 | grep -c "Cannot find module 'react-cytoscapejs'" should return 0</verify>
  <done>No test failures mentioning react-cytoscapejs or Cytoscape component</done>
</task>

<task type="auto">
  <name>Task 2: Fix Northwestern Animation test imports</name>
  <files>src/animations/__tests__/NorthwesternAnimations.test.js</files>
  <action>
1. Check if `src/animations/NorthwesternAnimations.js` exists - if deleted, the test file should be deleted
2. If the animation module was moved/renamed, update the test import path
3. If the animation module truly doesn't exist anymore, delete the test file `src/animations/__tests__/NorthwesternAnimations.test.js`

WHY: Tests for deleted modules cause suite failures. Either update imports or remove obsolete tests.
  </action>
  <verify>npm test -- --watchAll=false 2>&1 | grep -c "Cannot find module '../NorthwesternAnimations" should return 0</verify>
  <done>No test failures mentioning NorthwesternAnimations module</done>
</task>

<task type="auto">
  <name>Task 3: Clean up remaining broken test imports</name>
  <files>src/components/__tests__/*.test.js, src/hooks/__tests__/*.test.js</files>
  <action>
1. Run `npm test -- --watchAll=false 2>&1 | grep "Cannot find module"` to identify remaining broken imports
2. For each broken import:
   - If target file was moved: update import path
   - If target file was deleted: delete the test file
   - If target file was renamed: update import to new name
3. Focus only on "Cannot find module" errors (not TypeScript or assertion failures)

WHY: Test infrastructure must be able to load before we can improve test coverage. Import errors prevent this.
  </action>
  <verify>npm test -- --watchAll=false 2>&1 | grep "Cannot find module" | wc -l should be 0</verify>
  <done>All test suites can load without import errors. Individual tests may still fail but suites execute.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watchAll=false` runs without "Cannot find module" errors
- [ ] Cytoscape package removed from dependencies
- [ ] No orphaned test files for deleted components
- [ ] Test suite count should be ~66 passing (was 66 passing, 11 failing)
</verification>

<success_criteria>

- All test suites can execute (no module import failures)
- Cytoscape experimental code removed cleanly
- Test output shows only assertion/logic failures, not infrastructure failures
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-polish/07-01-SUMMARY.md`
</output>
