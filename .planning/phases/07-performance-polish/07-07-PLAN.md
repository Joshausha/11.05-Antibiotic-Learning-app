---
phase: 07-performance-polish
plan: 07
type: execute
---

<objective>
Enhance error handling with detailed development mode errors for better debugging and learning.

Purpose: When something breaks, show detailed information to help Josh understand and learn from errors.
Output: Enhanced ErrorBoundary with dev mode details, better error messages throughout.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-performance-polish/07-CONTEXT.md
@.planning/codebase/CONCERNS.md

**Current State:**
- ErrorBoundary exists at src/components/ErrorBoundary.tsx
- Uses console.error() for logging (CONCERNS.md notes this)
- No distinction between dev/prod error display
- Error messages are generic

**From 07-CONTEXT.md:**
"Detailed error information in development mode - when something breaks, show what went wrong so debugging and learning are easier"

**Environment Detection:**
- `process.env.NODE_ENV` available ('development' vs 'production')
- Can show detailed info in dev, minimal in prod
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance ErrorBoundary with development mode details</name>
  <files>src/components/ErrorBoundary.tsx</files>
  <action>
1. Read current ErrorBoundary implementation
2. Enhance to show detailed error info in development mode:
   - Error message
   - Error stack trace (formatted for readability)
   - Component stack (React-specific info about where error occurred)
   - Props/state at time of error (if available)
3. Keep production error display minimal and user-friendly
4. Add "Copy error details" button for easy bug reporting
5. Add "Try again" button to attempt recovery

Example dev mode display:
```tsx
{process.env.NODE_ENV === 'development' && (
  <details open>
    <summary>Error Details (Development Mode)</summary>
    <pre>{error.message}</pre>
    <pre>{error.stack}</pre>
    <pre>{componentStack}</pre>
  </details>
)}
```

WHY: Learning requires understanding errors. Detailed dev info accelerates debugging and learning.
  </action>
  <verify>Trigger an error in dev mode and verify detailed info displays</verify>
  <done>ErrorBoundary shows detailed error info in development mode</done>
</task>

<task type="auto">
  <name>Task 2: Add error boundary wrappers for key components</name>
  <files>src/App.tsx, src/components/ErrorBoundary.tsx</files>
  <action>
1. Create a lightweight error boundary wrapper component for sections:
   - Visualization errors shouldn't crash the whole app
   - Quiz errors shouldn't prevent exploring visualizations
   - Each major section can fail independently

2. Wrap key sections in App.tsx:
   - Visualization tab content
   - Quiz tab content
   - Comparison mode content

3. Each section error boundary should show:
   - Which section failed
   - Option to retry or dismiss
   - Not block other sections

WHY: Isolated error boundaries allow partial failures without full app crash.
  </action>
  <verify>Force an error in one section and verify other sections still work</verify>
  <done>Key sections have isolated error boundaries</done>
</task>

<task type="auto">
  <name>Task 3: Add helpful error messages to data loading</name>
  <files>src/data/EnhancedAntibioticData.ts, src/contexts/AppContext.tsx</files>
  <action>
1. Review data loading code for potential failure points
2. Add try/catch blocks with descriptive error messages:
   - "Failed to load antibiotic data: [specific reason]"
   - "Quiz question [id] has invalid format: [details]"
   - "Northwestern coverage data missing for [antibiotic]"

3. Use console.error with structured info in development:
```typescript
if (process.env.NODE_ENV === 'development') {
  console.error('Data loading error:', {
    file: 'EnhancedAntibioticData.ts',
    operation: 'loading antibiotics',
    error: e.message,
    data: problematicData
  });
}
```

4. Ensure errors bubble up to ErrorBoundary with useful info

WHY: "Failed to load data" is useless. "Failed to load Amoxicillin: missing gramPositiveCocci coverage" helps fix issues.
  </action>
  <verify>Introduce a data error and verify helpful message appears in console and ErrorBoundary</verify>
  <done>Data loading errors provide specific, actionable error messages</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ErrorBoundary shows detailed info in development mode
- [ ] ErrorBoundary shows minimal info in production mode
- [ ] Key sections have isolated error boundaries
- [ ] Data loading errors provide helpful messages
- [ ] npm test passes (error handling doesn't break tests)
</verification>

<success_criteria>

- Development mode errors include full details (message, stack, component info)
- Production mode errors are user-friendly
- Partial failures don't crash entire app
- Error messages help identify and fix issues
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-polish/07-07-SUMMARY.md`
</output>
