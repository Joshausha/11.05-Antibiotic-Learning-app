---
phase: 08-learning-analytics
plan: 02
type: execute
---

<objective>
Add weak spot identification and direct action links to the analytics dashboard.

Purpose: Show where you're struggling and provide one-click paths to study those areas
Output: WeakSpotsList component with actionable links, integrated into AnalyticsDashboard
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-learning-analytics/08-CONTEXT.md
@.planning/phases/08-learning-analytics/08-01-SUMMARY.md

# Key existing infrastructure:
@src/hooks/useQuizProgress.ts
# Already provides: stats.weakAreas (array of {topic, accuracy, totalQuestions})
# Topic extraction from questions already implemented in identifyWeakAreas()

@src/components/analytics/AnalyticsDashboard.tsx
@src/contexts/AppContext.tsx
@src/contexts/SharedSelectionContext.tsx

**Tech stack available:** React 18, TypeScript, Tailwind CSS
**Established patterns:** Mode switching via context, shared selection state

**Constraining decisions:**
- 08-CONTEXT: Weak spots are secondary to progress trajectory but still important
- 08-CONTEXT: Direct action links - one click to study weak area
- 06-02: SharedSelectionContext for cross-mode state

**Design requirements from 08-CONTEXT:**
- Show where you're struggling clearly
- Direct paths to study - one click action
- Board-prep aesthetic - not gamified
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WeakSpotsList component</name>
  <files>src/components/analytics/WeakSpotsList.tsx</files>
  <action>
Create a component that displays weak areas from useQuizProgress stats.

Props interface:
```tsx
interface WeakSpotsListProps {
  weakAreas: WeakArea[]; // {topic, accuracy, totalQuestions}
  onStudyTopic: (topic: string) => void;
}
```

Display each weak area as a row:
- Topic name (capitalized)
- Accuracy percentage with color coding:
  - < 50%: red background
  - 50-69%: orange/amber background
  - (70%+ wouldn't be in weak areas list)
- Question count: "Based on N questions"
- "Study" button that calls onStudyTopic(topic)

If weakAreas is empty, show: "No weak areas identified yet. Complete more quizzes to see personalized recommendations."

Styling: Professional, clean list. NO emojis. Gray borders, white cards pattern.
  </action>
  <verify>npm run build passes, component handles empty state and populated data</verify>
  <done>WeakSpotsList displays weak areas with accuracy color coding and study buttons</done>
</task>

<task type="auto">
  <name>Task 2: Integrate WeakSpotsList into AnalyticsDashboard with navigation</name>
  <files>src/components/analytics/AnalyticsDashboard.tsx, src/components/analytics/index.ts</files>
  <action>
1. Import and add WeakSpotsList to AnalyticsDashboard below the progress chart
2. Implement onStudyTopic handler that:
   - Uses AppContext to switch to 'quiz' tab
   - Passes topic filter (or stores it for quiz to pick up)

For the navigation:
- Import useAppContext and setActiveTab
- When "Study" is clicked, call setActiveTab('quiz')

Note: Full topic filtering in quiz would require additional work. For now, just navigate to quiz tab. The user can manually select topic-relevant questions. This is acceptable MVP behavior.

Update barrel file if needed.

Layout in dashboard:
```tsx
{/* After progress chart section */}
<div className="bg-white rounded-lg p-6 shadow-sm">
  <h2 className="text-lg font-semibold text-gray-800 mb-4">Areas to Focus On</h2>
  <WeakSpotsList
    weakAreas={stats.weakAreas}
    onStudyTopic={handleStudyTopic}
  />
</div>
```
  </action>
  <verify>npm run build passes, clicking Study navigates to quiz tab</verify>
  <done>WeakSpotsList integrated into dashboard with working navigation to quiz tab</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds
- [ ] WeakSpotsList renders weak areas with appropriate color coding
- [ ] Study button navigates to quiz tab
- [ ] Empty state message displays when no weak areas
- [ ] Professional board-prep aesthetic maintained
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Weak areas display clearly with actionable study links
- Phase 8 complete: Progress tracking + weak spot identification
</success_criteria>

<output>
After completion, create `.planning/phases/08-learning-analytics/08-02-SUMMARY.md` following the summary template.

Include "Phase 8 complete" in the summary since this completes Learning Analytics phase.
</output>
