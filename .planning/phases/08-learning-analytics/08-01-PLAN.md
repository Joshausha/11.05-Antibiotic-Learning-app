---
phase: 08-learning-analytics
plan: 01
type: execute
---

<objective>
Build the analytics dashboard with progress over time visualization - THE core of this phase.

Purpose: Show "Am I actually getting better?" with clear improvement trajectory visualization (UWorld-style professional, not gamified)
Output: AnalyticsDashboard component with progress chart, integrated into app navigation
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-learning-analytics/08-CONTEXT.md

# Key existing infrastructure:
@src/hooks/useQuizProgress.ts
# Already provides: stats.averageScore, stats.improvementTrend, stats.totalQuizzes,
# stats.recentQuizzes, quizHistory (array of CompletedQuiz objects with scorePercentage, completedAt)

@src/contexts/AppContext.tsx
@src/App.tsx
@src/types/app.types.ts

# Related patterns:
@.planning/phases/06-multi-modal-learning-flow/06-01-SUMMARY.md
# LearningHub pattern with mode cards

**Tech stack available:** React 18, TypeScript, Tailwind CSS, Chart.js (NOT D3 for simple charts - see data folder imports)
**Established patterns:** Multi-mode container tabs, hub & spoke navigation, React context

**Constraining decisions:**
- 06-01: Hub as first nav item - analytics should integrate cleanly
- Board-prep aesthetic: clean, professional, data-focused (per 08-CONTEXT)

**Design requirements from 08-CONTEXT:**
- Progress over time visualization is THE core feature
- UWorld-style analytics: clinical, data-dense, professional
- Not gamified - no streaks, no playful elements
- "Am I actually getting better?" should be answered at a glance
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create progress chart visualization</name>
  <files>src/components/analytics/ProgressChart.tsx</files>
  <action>
Create a line chart showing quiz scores over time. Use Chart.js (simpler than D3 for basic charts, already in package.json as chart.js).

Requirements:
- X-axis: time (dates of quizzes)
- Y-axis: score percentage (0-100%)
- Line showing score trend
- Optional: rolling average overlay to smooth the trend
- If fewer than 2 quizzes, show "Complete more quizzes to see your progress trend" message

Props interface:
- quizHistory: CompletedQuiz[] from useQuizProgress
- height?: number (default 300)

Style: Clean white background, professional colors (blue line for scores, gray grid), NO emojis, NO streaks display. Board-prep aesthetic.

Reference Chart.js React wrapper: 'react-chartjs-2' (if not in package.json, use vanilla Chart.js with useRef).
  </action>
  <verify>npm run build passes, component renders with mock data</verify>
  <done>ProgressChart displays line chart with quiz scores over time, handles empty state gracefully</done>
</task>

<task type="auto">
  <name>Task 2: Create AnalyticsDashboard container</name>
  <files>src/components/analytics/AnalyticsDashboard.tsx, src/components/analytics/index.ts</files>
  <action>
Create the main analytics dashboard component that:

1. Uses useQuizProgress hook to get stats and quizHistory
2. Displays headline metrics at top:
   - Total quizzes completed
   - Average score percentage
   - Improvement trend (improving/declining/stable as text badge, not gamified)
3. Progress chart as prominent center element (80%+ of vertical space)
4. Professional layout: white cards, gray borders, Tailwind styling

Structure:
```tsx
<div className="space-y-6 p-4">
  {/* Headline stats row */}
  <div className="grid grid-cols-3 gap-4">
    <StatCard label="Quizzes Completed" value={stats.totalQuizzes} />
    <StatCard label="Average Score" value={`${stats.averageScore}%`} />
    <StatCard label="Trend" value={stats.improvementTrend} />
  </div>

  {/* Main progress visualization */}
  <div className="bg-white rounded-lg p-6 shadow-sm">
    <h2 className="text-lg font-semibold text-gray-800 mb-4">Score Progress</h2>
    <ProgressChart quizHistory={quizHistory} />
  </div>
</div>
```

Create StatCard as internal helper or separate small component. NO emojis, NO playful copy. Clinical board-prep tone.

Also create barrel file src/components/analytics/index.ts exporting AnalyticsDashboard.
  </action>
  <verify>npm run build passes, component integrates useQuizProgress correctly</verify>
  <done>AnalyticsDashboard shows headline stats and progress chart, professional board-prep aesthetic</done>
</task>

<task type="auto">
  <name>Task 3: Add Analytics tab to app navigation</name>
  <files>src/types/app.types.ts, src/App.tsx</files>
  <action>
1. Add 'analytics' to TabType union in app.types.ts (after 'quiz')
2. Import AnalyticsDashboard in App.tsx
3. Add tab button to header navigation (icon: BarChart3 from lucide-react, or simple chart icon)
4. Add conditional render for analytics tab content

Position analytics tab AFTER quiz in navigation order since it's about tracking quiz progress.

Match existing tab patterns in App.tsx.
  </action>
  <verify>npm run build passes, analytics tab appears in navigation, clicking it shows dashboard</verify>
  <done>Analytics tab accessible from main navigation, renders AnalyticsDashboard</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds
- [ ] Analytics tab appears in header navigation
- [ ] ProgressChart renders (with mock data or empty state if no quiz history)
- [ ] Dashboard shows headline stats from useQuizProgress
- [ ] Professional board-prep aesthetic (no gamification elements)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors related to new components
- Progress over time is the dominant visual element
- Clinical, professional tone throughout
</success_criteria>

<output>
After completion, create `.planning/phases/08-learning-analytics/08-01-SUMMARY.md` following the summary template.
</output>
